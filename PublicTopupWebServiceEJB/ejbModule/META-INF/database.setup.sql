CREATE TABLE TOPUPWS.MobileNetworkOperator (mnoID VARCHAR(30) NOT NULL, mnoName VARCHAR(60), PRIMARY KEY (mnoID)) ENGINE = innodb;
CREATE TABLE TOPUPWS.OPENJPA_SEQUENCE_TABLE (ID TINYINT NOT NULL, SEQUENCE_VALUE BIGINT, PRIMARY KEY (ID)) ENGINE = innodb;
CREATE TABLE TOPUPWS.TransactionInfo (bankId VARCHAR(30) NOT NULL, transactionType VARCHAR(30) NOT NULL, uuid VARCHAR(30) NOT NULL, airtimeBalance DOUBLE, amount BIGINT, dateReceived DATETIME, initialBalance DOUBLE, sourceMobileNumber VARCHAR(32), targetMobileNumber VARCHAR(32), mobileOperatorId VARCHAR(12), narrative VARCHAR(255), notify BIT, operatorReference VARCHAR(50), responseCode VARCHAR(4), serviceId VARCHAR(20), serviceProviderId VARCHAR(30), status VARCHAR(32), valueDate DATETIME, targetMNO VARCHAR(255), PRIMARY KEY (bankId, transactionType, uuid)) ENGINE = innodb;
